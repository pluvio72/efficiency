# Arrays

An array is a linear data structure which stores a number of elements in a specific order. Individual elements are accessed with an integer index. Arrays can be of a fixed size or re-sizeable. Since arrays are very versatile they can be used for most use cases.
